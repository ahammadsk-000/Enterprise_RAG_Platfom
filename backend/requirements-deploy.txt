# Runtime deps for the Render free-tier deploy. Fits within 512MB RAM:
# real LLM via Groq (OpenAI-compatible httpx) + Postgres + in-memory vector/graph.
# No torch, no Qdrant client, no Neo4j driver, no boto3, no langchain.
#
# pymupdf + python-docx are included so PDF + DOCX uploads still work end-to-end
# (text-based; scanned PDFs need OCR which is null in LITE_MODE).

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pyjwt[crypto]>=2.10
passlib[argon2]>=1.7
authlib>=1.3
itsdangerous>=2.2
structlog>=24.4
httpx>=0.27
python-multipart>=0.0.12
email-validator>=2.2
celery>=5.4
prometheus-client>=0.21
pymupdf>=1.24
python-docx>=1.1
