# Lightweight runtime deps for DEMO MODE (native Python, fake LLM/embeddings,
# in-memory vector/graph/storage, inline ingestion). Excludes the heavy AI/ML and
# document libs (torch, transformers, pymupdf, langchain, qdrant, neo4j, boto3) —
# parsers/providers import those lazily and aren't exercised in demo mode.
#
# Demo supports text/markdown/HTML uploads; PDF/DOCX/image need the full install.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pyjwt[crypto]>=2.10
passlib[argon2]>=1.7
authlib>=1.3
itsdangerous>=2.2
structlog>=24.4
httpx>=0.27
python-multipart>=0.0.12
email-validator>=2.2
celery>=5.4
prometheus-client>=0.21
